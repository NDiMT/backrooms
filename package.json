{
  "name": "nekri-zoni",
  "version": "1.0.0",
  "description": "ΝΕΚΡΗ ΖΩΝΗ — Doom-style roguelike FPS στον browser / Android",
  "private": true,
  "scripts": {
    "build": "node scripts/build-www.js",
    "sync": "npm run build && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
